# params_template.yaml
# 植物生信分析运行参数模板

outdir: "./outputs"

# 决策模式 (必选)
# rule   — 严格按决策矩阵执行
# expert — Claude 自主推理
# hybrid — 规则优先，无匹配时推理 (推荐)
decision_mode: hybrid

# 分析参数 (按需修改)
maf_threshold: 0.05
geno_threshold: 0.1
mind_threshold: 0.1
hwe_threshold: 1e-6

# 物种 (可选，用于加速物种识别)
# species: maize
